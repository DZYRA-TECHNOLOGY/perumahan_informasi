# Salin file ini menjadi .env lalu isi dari Supabase → Project Settings → API.
# Jika dibiarkan kosong, aplikasi otomatis memakai data lokal (demo).
# Boleh pakai kunci baru (PUBLISHABLE_KEY) atau lama (ANON_KEY).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
